/**
 * Effet splash cursor WebGL — module centralisé pour tous les héros Lab Horizon.
 *
 * Usage :
 *   import { HeroGlassBackground } from '../lib/heroGlass'
 *   <HeroGlassBackground imageSrc={src} heroRef={heroRef} />
 */
export { HeroGlassBackground } from './HeroGlassBackground'
export { getHeroGlassConfig, heroGlassConfig, MAX_SPLASHES } from './config'
export type { HeroGlassConfig } from './config'
